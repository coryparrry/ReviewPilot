Miss calibration focus:
Fresh live misses to bias toward:
- Keep listUserProfilesByIds() immutable like the other read path in packages/core-service/src/workspace/user-profile-repository.ts: listAllUserProfiles() returns cloned users, but this method currently forwards packages/core-service/src/workspace/user-profile-store-adapter.ts:35-38, which returns the original s. Evidence anchors to mention if present: listAllUserProfiles() returns cloned users, but this method currently forwards packages/core-service/src/workspace/user- | ts:35-38, which returns the original store | users entries
Accepted CodeRabbit comment patterns worth preserving:
- doc-sync: Mirror the active execution-plan rewrite into docs/exec-plans/active/current.md.
- portability: Print a shell-neutral next-step pipeline command instead of a Windows-only path.
- runtime-correctness: Fix gh api pagination so json.loads does not receive multiple JSON documents.
- input-validation: Fail closed on malformed candidate rows instead of silently dropping them.
- failure-reporting: Always write automation-summary.json even if one child step fails.
- safety-boundary: The bounded fix executor cannot really enforce a file allowlist under workspace-write mode.
